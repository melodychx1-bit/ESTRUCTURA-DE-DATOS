/*Ejercicio 42. Ordenamiento burbuja descendente
Enunciado. Ordenar N enteros de mayor a menor implementando Bubble Sort manualmente.*/


#include <iostream>
using namespace std;

int main() {
    int numeros[50];
    int N;
    int aux;

    cout << "Ingrese la cantidad de numeros: ";
    cin >> N;

    cout << "Ingrese los " << N << " numeros: ";
    for (int i = 0; i < N; i++) {
        cin >> numeros[i];
    }

    for (int i = 0; i < N - 1; i++) {
        for (int j = 0; j < N - 1 - i; j++) {
            if (numeros[j] < numeros[j + 1]) {
                aux = numeros[j];
                numeros[j] = numeros[j + 1];
                numeros[j + 1] = aux;
            }
        }
    }

    cout << "Arreglo ordenado de mayor a menor: ";

    for (int i = 0; i < N; i++) {
        cout << numeros[i] << " ";
    }

    return 0;
}
